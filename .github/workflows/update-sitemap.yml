name: 🗺️ Автооновлення sitemap.xml

on:
  workflow_dispatch:   # вручну
  schedule:
    - cron: '0 2 * * *'  # щодня о 2:00 ночі (UTC)

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🔽 Клон репозиторій
        uses: actions/checkout@v3

      - name: ⚙️ Встановити Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: 📦 Встановити залежності (нічого немає)
        run: npm init -y

      - name: 🧠 Запустити генерацію sitemap.xml
        run: node generate-sitemap.js

      - name: 💾 Закомітити та запушити sitemap.xml
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add sitemap.xml
          git commit -m "🔄 Автооновлення sitemap.xml" || echo "Немає змін"
          git push
