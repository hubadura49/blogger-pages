name: 🗺️ Автооновлення sitemap.xml

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest  # ← ОБОВʼЯЗКОВО!
    steps:
      - name: 🔽 Клонувати репозиторій
        uses: actions/checkout@v3

      - name: 🧠 Встановити Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 📦 Встановити залежності
        run: npm install

      - name: ⚙️ Запустити генерацію sitemap.xml
        run: node generate-sitemap.js

      - name: 💾 Закомітити sitemap.xml
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add sitemap.xml
          git diff --cached --quiet || git commit -m "🔄 Автооновлення sitemap.xml"
          git push
